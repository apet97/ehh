[project]
name = "clankerbot"
version = "0.2.1"
description = "Webhook/Cron → API calls, with optional LLM parsing (DeepSeek)."
requires-python = ">=3.10"
readme = "README.md"
license = {file = "LICENSE"}

[tool.black]
line-length = 100

[tool.ruff]
line-length = 100
